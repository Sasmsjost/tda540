package towerdefence.graphics;

import towerdefence.util.Position;

/**
 * Represents a position in a grid
 */
public class ScreenPosition extends Position<Float> {
    public ScreenPosition(float x, float y) {
        super(x, y);
    }
}
